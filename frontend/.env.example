# 后端公网地址。生产环境必须替换为实际的 HTTPS 地址。
VITE_API_BASE_URL=http://localhost:8000

# 部署到 GitHub Pages 子目录时填写仓库路径，例如 /ai-resume-analyzer/
VITE_BASE_PATH=/

# 仅供本地开发代理使用
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
